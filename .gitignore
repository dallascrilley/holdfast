node_modules/
# Direct dependency versions are pinned exactly in package.json; the lockfile is
# regenerated per environment rather than committed.
package-lock.json
dist/
.env
*.log
.DS_Store
coverage/
